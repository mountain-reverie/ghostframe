[package]
name = "ghostframe-cli"
version = "0.1.0"
edition = "2021"

[lib]
name = "ghostframe_cli"
path = "src/lib.rs"

[[bin]]
name = "ghostframe"
path = "src/main.rs"

# No dependencies until M2. Depending on ghostframe-client-capi now would
# pull in ghostframe-tsnet, whose build.rs requires the built web SPA, making
# a seven-line stub unbuildable in a fresh checkout. They are one line each
# to add when the CLI is actually implemented.
[dependencies]
