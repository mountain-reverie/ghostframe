{
  "name": "ghostframe-web-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "@webgpu/types": "^0.1.40",
    "typescript": "^5.5",
    "vite": "^6.0",
    "vitest": "^4.1"
  }
}
